pipeline {
    agent any
    stages {
        stage('clone-repo') {
            echo "clonning"
        }
        stage('test') {
            echo "testing app"
        }
        stage('buid') {
            echo "building app"
        }
    }
}

